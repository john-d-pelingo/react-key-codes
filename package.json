{
  "name": "react-key-codes",
  "version": "0.0.6",
  "description": "Reveal the JavaScript key codes on key down. Built with react and friends. Bootstrapped with the help of Facebook's create-react-app.",
  "private": false,
  "homepage": "https://john-d-pelingo.github.io/react-key-codes",
  "author": "John Darryl Pelingo <johndarrylpelingo@gmail.com> (https://github.com/john-d-pelingo)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/john-d-pelingo/react-key-codes.git"
  },
  "scripts": {
    "build": "run-s build:js",
    "build:coverage": "run-s build:js copy-circle-ci-yml",
    "build:css": "node-sass --output-style expanded --indent-width 4 --linefeed lf --include-path scss src/static/scss/style.scss src/static/scss/style.css",
    "build:js": "cross-env NODE_PATH=src react-scripts build",
    "clean": "rimraf build coverage",
    "copy-circle-ci-yml": "node scripts/copy-circle-ci-yml.js",
    "deploy": "gh-pages  -m 'Updates [ci skip]' -d build",
    "deploy:ready": "gh-pages  -m 'Updates [ci skip]' -d build",
    "eject": "cross-env NODE_PATH=src react-scripts eject",
    "lint": "eslint -c .eslintrc.yml *.js src",
    "prebuild": "run-s build:css clean lint test:deploy",
    "prebuild:coverage": "run-s build:css clean lint test:coverage",
    "predeploy": "npm run build",
    "start": "run-p watch:css start:js",
    "start:js": "cross-env NODE_PATH=src react-scripts start",
    "test": "cross-env NODE_PATH=src react-scripts test --env=jsdom",
    "test:deploy": "cross-env NODE_PATH=src CI=true react-scripts test --env=jsdom",
    "test:coverage": "cross-env NODE_PATH=src CI=true react-scripts test --env=jsdom --coverage --no-cache && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "watch:css": "nodemon -e scss -x \"npm run build:css\"",
    "watch:lint": "esw -c .eslintrc.yml *.js src --color -w"
  },
  "keywords": [
    "create-react-app",
    "ci",
    "enzyme",
    "es6",
    "eslint",
    "jest",
    "jsx",
    "react",
    "scss"
  ],
  "dependencies": {
    "prop-types": "15.5.10",
    "react": "15.5.4",
    "react-dom": "15.5.4",
    "react-ga": "2.2.0",
    "sieses": "0.1.4"
  },
  "devDependencies": {
    "coveralls": "2.13.1",
    "cross-env": "5.0.0",
    "enzyme": "2.8.2",
    "eslint": "3.19.0",
    "eslint-config-airbnb": "15.0.1",
    "eslint-plugin-import": "2.3.0",
    "eslint-plugin-jsx-a11y": "5.0.3",
    "eslint-plugin-react": "7.0.1",
    "eslint-watch": "3.1.0",
    "fs-extra": "3.0.1",
    "gh-pages": "1.0.0",
    "node-sass": "4.5.3",
    "nodemon": "1.11.0",
    "npm-run-all": "4.0.2",
    "react-scripts": "1.0.6",
    "react-test-renderer": "15.5.4",
    "rimraf": "2.6.1"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/*.js",
      "!src/core/*.js",
      "!src/core/**/index.js",
      "!src/views/**/index.js",
      "!<rootDir>/node_modules/"
    ]
  }
}
